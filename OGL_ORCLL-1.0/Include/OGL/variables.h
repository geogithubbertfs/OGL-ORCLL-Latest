#pragma once

// This is OGL's native character variable
typedef char olChar;

// This is OGL's native string variable
typedef char* olString;

// This is OGL's native integer variable
typedef int olInt;

// This is OGL's native float variable
typedef float olFloat;

// This is OGL's native double variable
typedef double olDouble;


// for the sake of not writting to much lines of code with enums
#define olEnum typedef enum

// if needed:
#define olStruct typedef struct 